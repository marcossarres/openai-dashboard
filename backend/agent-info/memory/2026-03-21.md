## Sarrescost backend deployment
- Created AWS Secrets Manager secret `sarrescost/backend/credentials` (account 915759771410, us-east-1) to hold the backend's AWS keys; the OpenAI key will be supplied later from the frontend settings UI.
- Updated the ECS task role policy + template so tasks read/write that secret (`secretsmanager:Describe/Get/Put`) and expose the `CREDENTIALS_SECRET_ID` env var for the backend.
- Because the cluster only has one t3.medium host, the ECS service now uses maxPercent=100/minHealthyPercent=0 and is scaled to 0 while the stack rollback finishes and we reapply the template (including enabling AssignPublicIp so tasks can reach Secrets Manager/OpenAI).
